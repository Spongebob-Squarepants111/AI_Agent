# BCI Assistant - 脑机接口智能助手

一个基于 OpenAI + LangChain 的脑机接口（Brain-Computer Interface）智能助手系统，旨在为脑机接口用户提供智能化的交互体验和辅助功能。

## 📋 项目简介

本项目为脑机接口应用场景提供了一个可扩展的 AI 助手后端服务，支持：
- 🧠 基于 OpenAI GPT 模型的智能对话与问答
- 📚 RAG 知识增强（支持 PDF 等文档上传和向量检索）
- 💾 会话记忆管理（使用 Redis 存储对话历史）
- 🔍 网络搜索工具（集成 SerpAPI）
- 🔊 文本转语音（TTS）功能
- 🎭 可配置的个性化和情绪化回答
- 🌐 RESTful API 接口，易于集成

## 🎯 应用场景

### 核心功能
- **智能问答**：回答用户的各类问题，提供准确、友好的回复
- **文档知识增强（RAG）**：上传 PDF 等文档，通过向量检索增强回答的专业性和准确性
- **会话记忆**：使用 Redis 存储对话历史，支持多轮对话和上下文理解
- **网络搜索**：集成 SerpAPI，实时获取互联网最新信息
- **文本转语音（TTS）**：将文字回复转换为语音，提供更自然的交互体验
- **个性化助手**：可配置助手的性格特点和情绪化回答风格，打造独特的交互体验

### 脑机接口应用场景
- **辅助交流**：为 BCI 用户提供自然语言交互界面，降低操作复杂度
- **知识获取**：快速查询和学习专业知识，支持文档上传和检索
- **信息检索**：通过网络搜索获取实时信息
- **情感陪伴**：通过个性化和情绪化的回答，提供更温暖的交互体验

## 🏗️ 技术架构

```
┌─────────────────────┐
│   BCI 前端设备       │  (脑机接口硬件/软件)
└──────┬──────────────┘
       │ HTTP/WebSocket
┌──────▼──────────────────────────┐
│      FastAPI 服务器              │
├─────────────────────────────────┤
│  LangChain Agent 框架            │
│  ├─ OpenAI LLM (GPT-3.5/4)     │
│  ├─ ReAct Agent (推理+行动)     │
│  ├─ 会话记忆 (Redis)             │
│  ├─ 知识库 (RAG + Qdrant)       │
│  └─ 工具链 (可扩展)              │
└─────────────────────────────────┘
```

### 核心组件

- **FastAPI**: 高性能 Web 框架，提供 RESTful API
- **LangChain**: AI 应用开发框架，管理 LLM 调用和 Agent 逻辑
- **OpenAI API**: GPT 系列模型，提供强大的语言理解和生成能力
- **Redis**: 内存数据库，管理会话历史和用户状态
- **Qdrant**: 向量数据库，存储和检索 BCI 相关知识（可选）

## ✨ 主要功能

### 1. 智能对话
- 支持多轮对话，自动维护上下文
- 会话记忆管理，每个用户独立会话
- 意图识别与理解，适配 BCI 交互特点

### 2. Agent 工具链
- ReAct Agent 架构，支持推理和行动
- 可扩展的工具系统：
  - 🔍 知识库检索工具（RAG）
  - 🌐 网络搜索工具（SerpAPI）
  - 🔊 文本转语音工具（Edge TTS）
  - 🛠️ 自定义工具（可扩展）

### 3. 知识库支持（RAG）
- 文档上传与处理（PDF、文本、网页等）
- 文档向量化存储（Qdrant）
- 语义相似度检索
- 上下文增强生成

### 4. 个性化配置
- 可配置助手性格特点
- 支持情绪化回答风格
- 基于会话历史的个性化交互

### 5. API 接口

#### 基础对话接口
- `/chat` - 带记忆的智能对话接口（推荐）
- `/simple_chat` - 简单对话接口
- `/chat/history` - 获取会话历史
- `/chat/history` (DELETE) - 清空会话历史

#### RAG 知识库接口
- `/knowledge/add_text` - 添加文本到知识库
- `/knowledge/add_pdf` - 添加 PDF 文件到知识库
- `/knowledge/add_url` - 从 URL 添加内容到知识库
- `/knowledge/search` - 搜索知识库
- `/knowledge/info` - 获取知识库信息
- `/chat_with_knowledge` - 使用知识库增强的对话

#### 搜索工具接口
- `/search` - 网络搜索接口（使用 SerpAPI）
- `/chat_with_search` - 使用网络搜索增强的对话

## 🚀 快速开始

### 环境要求

- Python 3.11+
- Redis 服务器
- Qdrant 向量数据库（可选，用于知识库功能）

### 安装步骤

1. **进入项目目录**
```bash
cd /root/Ai_Agent/Ai_Agent
```

2. **安装依赖**
```bash
# 使用 conda 环境（推荐）
conda activate agent
pip install -r requirements.txt

# 或使用 pip
pip install -r requirements.txt
```

3. **配置环境变量**

编辑 `.env` 文件：

```bash
# OpenAI API 配置
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_API_BASE=https://api.openai.com/v1  # 可选，使用代理时配置
OPENAI_MODEL=gpt-3.5-turbo  # 或 gpt-4

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Qdrant 向量数据库配置（可选）
QDRANT_HOST=localhost
QDRANT_PORT=6333

# SerpAPI 配置（网络搜索功能）
SERPAPI_KEY=your_serpapi_key_here

# SerpAPI 配置（可选，用于网络搜索）
SERPAPI_KEY=your_serpapi_key_here

# Agent 配置
MAX_ITERATIONS=3  # Agent 最大迭代次数
TEMPERATURE=0.7   # 模型温度参数
MAX_HISTORY=10    # 最大历史记录数
```

4. **启动 Redis**
```bash
redis-server
```

5. **启动 Qdrant（可选）**
```bash
docker run -p 6333:6333 qdrant/qdrant
```

6. **启动服务**
```bash
python server.py
```

服务将在 `http://localhost:8000` 启动。

### 验证安装

访问 API 文档：
```
http://localhost:8000/docs
```

## 📖 使用指南

### 1. 简单对话

```python
import requests

response = requests.post(
    "http://localhost:8000/simple_chat",
    params={"query": "你好，我想了解脑机接口技术"}
)
print(response.json())
```

### 2. 带会话记忆的对话（推荐）

```python
import requests

# 第一轮对话
response1 = requests.post(
    "http://localhost:8000/chat",
    params={
        "query": "我想了解脑机接口的应用场景",
        "session_id": "user_001"
    }
)

# 第二轮对话（会记住上下文）
response2 = requests.post(
    "http://localhost:8000/chat",
    params={
        "query": "它在医疗领域有哪些应用？",
        "session_id": "user_001"
    }
)
```

### 3. 获取会话历史

```python
response = requests.get(
    "http://localhost:8000/chat/history",
    params={"session_id": "user_001"}
)
print(response.json())
```

### 4. 网络搜索

```python
# 直接搜索
response = requests.get(
    "http://localhost:8000/search",
    params={
        "query": "Python programming language",
        "num_results": 5
    }
)
print(response.json())

# 使用搜索增强的对话
response = requests.post(
    "http://localhost:8000/chat_with_search",
    params={
        "query": "What are the latest features in Python 3.12?",
        "session_id": "user_001",
        "num_results": 3
    }
)
print(response.json())
```

### 5. RAG 知识库管理

```python
# 添加文本到知识库
response = requests.post(
    "http://localhost:8000/knowledge/add_text",
    params={
        "text": "脑机接口（BCI）是一种直接在大脑和外部设备之间建立通信通道的技术...",
        "metadata": '{"source": "manual", "topic": "BCI"}'
    }
)

# 搜索知识库
response = requests.get(
    "http://localhost:8000/knowledge/search",
    params={
        "query": "脑机接口的应用",
        "k": 3
    }
)

# 使用知识库增强的对话
response = requests.post(
    "http://localhost:8000/chat_with_knowledge",
    params={
        "query": "脑机接口在医疗领域有哪些应用？",
        "session_id": "user_001",
        "k": 3
    }
)
```

### 6. 清空会话历史

```python
response = requests.delete(
    "http://localhost:8000/chat/history",
    params={"session_id": "user_001"}
)
```

## 🔧 配置说明

### OpenAI 模型配置

在 `.env` 中可以配置使用的模型：

```bash
# 推荐模型
OPENAI_MODEL=gpt-4-turbo-preview  # 最强性能
OPENAI_MODEL=gpt-4                # 平衡性能和成本
OPENAI_MODEL=gpt-3.5-turbo        # 快速响应，成本低（默认）
```

### Agent 参数调优

```bash
# Agent 最大迭代次数（推理-行动循环）
MAX_ITERATIONS=3  # 建议 3-5

# 模型温度参数（创造性）
TEMPERATURE=0.7  # 0-1 之间，越高越有创造性

# 会话历史记录数量
MAX_HISTORY=10  # 建议 5-20
```

## 📁 项目结构

```
Ai_Agent/
├── server.py              # FastAPI 服务器主文件
├── config/
│   ├── __init__.py
│   └── settings.py        # 配置管理
├── memory/
│   ├── __init__.py
│   └── session_memory.py  # 会话记忆管理
├── requirements.txt       # Python 依赖
├── .env                   # 环境变量配置
└── README.md             # 项目文档
```

## 🔐 安全建议

1. **保护 API Key**
   - 不要将 `.env` 文件提交到版本控制
   - 定期轮换 API Key
   - 使用环境变量管理敏感信息

2. **访问控制**
   - 在生产环境中添加身份验证
   - 限制 API 调用频率
   - 使用 HTTPS 加密通信

3. **数据安全**
   - 定期备份 Redis 数据
   - 对敏感的用户数据进行加密存储
   - 实施数据访问审计

## 🎯 开发路线图

### 当前版本 (v1.0)
- ✅ 基础对话功能
- ✅ Agent 工具链架构
- ✅ 会话记忆管理
- ✅ OpenAI 集成

### 计划功能
- [ ] 完善 RAG 文档上传接口（PDF、文本、网页）
- [ ] 实现 SerpAPI 网络搜索工具
- [ ] 集成 Edge TTS 文本转语音功能
- [ ] 添加助手性格和情绪配置系统
- [ ] 实时流式响应
- [ ] 多模态输入支持（图片、音频）
- [ ] Web UI 管理界面
- [ ] 用户认证与权限系统
- [ ] 对话历史分析与导出
- [ ] 知识库管理界面

### BCI 特定功能扩展
- [ ] BCI 信号数据接入与处理
- [ ] 意图识别优化（针对 BCI 场景）
- [ ] BCI 设备状态监控工具
- [ ] 用户疲劳度检测工具
- [ ] 个性化训练建议工具

## 🐛 常见问题

### Q: OpenAI API 调用失败？
**A:** 检查以下几点：
1. API Key 是否正确配置
2. 是否有足够的配额
3. 网络连接是否正常（国内可能需要代理）
4. API Base URL 是否正确

### Q: Redis 连接失败？
**A:** 确保 Redis 服务正在运行：
```bash
redis-cli ping  # 应返回 PONG
```

### Q: 如何使用代理访问 OpenAI？
**A:** 在 `.env` 中配置：
```bash
# 方式1: 使用代理
HTTP_PROXY=http://your-proxy:port
HTTPS_PROXY=http://your-proxy:port

# 方式2: 使用 API 中转服务（推荐）
OPENAI_API_BASE=https://your-proxy-api.com/v1
```

### Q: 如何扩展自定义工具？
**A:** 在 `server.py` 中使用 `@tool` 装饰器定义新工具：
```python
from langchain.agents import tool

@tool
def your_custom_tool(input: str) -> str:
    """工具描述"""
    # 实现你的工具逻辑
    return result
```

## 📚 参考资源

- [OpenAI API 文档](https://platform.openai.com/docs)
- [LangChain 文档](https://python.langchain.com/)
- [FastAPI 文档](https://fastapi.tiangolo.com/)
- [Redis 文档](https://redis.io/documentation)
- [Qdrant 文档](https://qdrant.tech/documentation/)

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

**开始构建你的脑机接口智能助手吧！** 🧠🤖
